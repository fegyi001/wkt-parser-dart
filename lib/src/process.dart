dynamic mapit(obj, key, value) {
  // if (Array.isArray(key)) {
  //   value.unshift(key);
  //   key = null;
  // }
  // var thing = key ? {} : obj;

  // var out = value.reduce(function(newObj, item) {
  //   sExpr(item, newObj);
  //   return newObj
  // }, thing);
  // if (key) {
  //   obj[key] = out;
  // }
}
